const assert = requiire('assert.js');
const calculateNumber = require("./0-calcul.js");

describe('test suite 1', calculateNumber() {
  it('adds numbers' , calculateNumber() {
    assert.equal(calculateNumber(1, 3), 4);
  });
});
